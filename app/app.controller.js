'use strict';

angular.module('andruclient').controller('AndruclietnController', AndruclientController);

AndruclientController.$inject = ['$state', 'principal', '$rootScope', 'menuService'];

function AndruclientController($state, principal, $rootScope, menuService) {

}
